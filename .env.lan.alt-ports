# OpenCode AG-UI 區網訪問 - 替代端口配置
# 自動生成於 西元2026年01月18日 (週日) 18時08分41秒 CST
# 解決與JupyterHub的端口衝突

# 網路配置
HOST_IP=
LAN_FRONTEND_URL=http://:8080
LAN_BACKEND_URL=http://:3001
LAN_WS_URL=ws://:3001

# 應用配置 - 替代端口
VITE_OPENCODE_API_URL=http://:3001
VITE_OPENCODE_WS_URL=ws://:3001

# 開發/生產模式
NODE_ENV=production
HOST=0.0.0.0
PORT=3001

# 資料庫 (保持不變)
DATABASE_URL=postgresql://opencode:${POSTGRES_PASSWORD}@postgres:5432/opencode
REDIS_URL=redis://redis:6379

# 認證
JWT_SECRET=${JWT_SECRET:-change_this_secret_in_production}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-change_this_password}
